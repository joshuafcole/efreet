{
  "name": "efreet",
  "version": "0.1.0",
  "description": "A lightweight, alternative web framework based around microReact",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f *.js",
    "build": "tsc",
    "publish": "npm run clean; npm run build"
  },
  "author": "Joshua Cole",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^9.3.0",
    "typescript": "^2.6.2"
  }
}
